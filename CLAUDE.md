
# Project hints
- Use the word "create" instead of "mint" when creating something.
- Run pytest with --testmon to speed up red-green testing.

# graphify
- Use the graphify mcp server to search for code instead of Grep and loading entire files.
